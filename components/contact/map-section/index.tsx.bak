"use client";

import Link from "next/link";
import { MapPin } from "lucide-react";
import { motion, useReducedMotion } from "framer-motion";
import { Button } from "@/components/ui/button";

export function MapSection() {
  const shouldReduceMotion = useReducedMotion();

  return (
    <section
      className="relative w-full py-12 md:py-16 lg:py-20 bg-gray-50 dark:bg-slate-900/50"
      aria-label="Store location map"
    >
      <div className="container mx-auto px-4 sm:px-6 lg:px-8 max-w-7xl">
        {/* Section Header */}
        <motion.div
          initial={{ opacity: 0, y: shouldReduceMotion ? 0 : 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ duration: shouldReduceMotion ? 0 : 0.5 }}
          className="text-center mb-8 md:mb-12"
        >
          <h2 className="text-3xl md:text-4xl font-bold text-gray-900 dark:text-gray-50 mb-3">
            Find Us
          </h2>
          <p className="text-gray-600 dark:text-gray-300 max-w-2xl mx-auto">
            Visit our main location or find the nearest Pizza Space store to you
          </p>
        </motion.div>

        {/* Map Container */}
        <motion.div
          initial={{ opacity: 0, scale: shouldReduceMotion ? 1 : 0.95 }}
          whileInView={{ opacity: 1, scale: 1 }}
          viewport={{ once: true }}
          transition={{
            duration: shouldReduceMotion ? 0 : 0.6,
            delay: shouldReduceMotion ? 0 : 0.2,
            ease: "easeOut",
          }}
          className="relative rounded-2xl overflow-hidden shadow-lg dark:shadow-2xl mb-8"
        >
          {/* Static Map Placeholder - Replace with Google Maps embed when ready */}
          <div className="relative w-full h-[400px] md:h-[500px] bg-gradient-to-br from-gray-200 to-gray-300 dark:from-slate-800 dark:to-slate-700">
            {/* Map Icon Overlay */}
            <div className="absolute inset-0 flex flex-col items-center justify-center">
              <motion.div
                initial={{ scale: shouldReduceMotion ? 1 : 0, opacity: 0 }}
                whileInView={{ scale: 1, opacity: 1 }}
                viewport={{ once: true }}
                transition={{
                  duration: shouldReduceMotion ? 0 : 0.5,
                  delay: shouldReduceMotion ? 0 : 0.4,
                  type: "spring",
                  stiffness: 200,
                  damping: 15,
                }}
                className="w-20 h-20 bg-orange-500 dark:bg-orange-600 rounded-full flex items-center justify-center shadow-xl mb-4"
              >
                <motion.div
                  animate={shouldReduceMotion ? {} : {
                    y: [0, -5, 0],
                  }}
                  transition={{
                    duration: 2,
                    repeat: Infinity,
                    ease: "easeInOut",
                  }}
                >
                  <MapPin className="w-10 h-10 text-white" />
                </motion.div>
              </motion.div>
              <motion.h3
                initial={{ opacity: 0, y: shouldReduceMotion ? 0 : 10 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{
                  duration: shouldReduceMotion ? 0 : 0.5,
                  delay: shouldReduceMotion ? 0 : 0.6,
                }}
                className="text-2xl font-bold text-gray-900 dark:text-gray-50 mb-2"
              >
                Pizza Space HQ
              </motion.h3>
              <motion.p
                initial={{ opacity: 0, y: shouldReduceMotion ? 0 : 10 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{
                  duration: shouldReduceMotion ? 0 : 0.5,
                  delay: shouldReduceMotion ? 0 : 0.7,
                }}
                className="text-gray-700 dark:text-gray-300 text-center"
              >
                123 Pizza Lane, London, UK EC1A 1BB
              </motion.p>
            </div>

            {/* Google Maps Embed - Uncomment when ready to use
            <iframe
              src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d2483.0965332!2d-0.127758!3d51.50735!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x0%3A0x0!2zNTHCsDMwJzI2LjUiTiAwwrAwNyczNS45Ilc!5e0!3m2!1sen!2suk!4v1234567890123!5m2!1sen!2suk"
              width="100%"
              height="100%"
              style={{ border: 0 }}
              allowFullScreen
              loading="lazy"
              referrerPolicy="no-referrer-when-downgrade"
              title="Pizza Space HQ Location"
              aria-label="Map showing Pizza Space HQ location"
            />
            */}
          </div>
        </div>
        {/* Find Nearest Store Button */}
        <motion.div
          initial={{ opacity: 0, y: shouldReduceMotion ? 0 : 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{
            duration: shouldReduceMotion ? 0 : 0.5,
            delay: shouldReduceMotion ? 0 : 0.3,
          }}
          className="text-center"
        >
          <Button
            asChild
            size="lg"
            className="bg-orange-500 hover:bg-orange-600 dark:bg-orange-600 dark:hover:bg-orange-700 text-white"
          >
            <Link href="/stores">
              <MapPin className="mr-2 h-5 w-5" />
              Find Nearest Store
            </Link>
          </Button>
        </motion.div>
      </div>
    </section>
  );
}
