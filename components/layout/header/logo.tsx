/**
 * Logo component - Re-exported from logo-client for backward compatibility
 * Note: The actual logo fetching is now handled in the Header component
 * Use LogoClient directly for client-side usage with pre-fetched URLs
 */
export { LogoClient as Logo } from "./logo-client";
