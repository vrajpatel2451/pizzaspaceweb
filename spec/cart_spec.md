# Cart Feature development

1. Added cart apis and types for cart to operate
2. Get cart api should fetched from every page initial load to get global data
3. device Id and store Id to be compulsary and that to be global state
4. deviceId should be random generated and should be accessible global state
5. store Id should be global and should be selected via select store in the header
6. Cart screen should have item list, apply discount then delivery type order delivery type with addresses
7. Need address screen with add address and update address and delete address
8. then in right side summary api call should be happening in every cart changes
9. cart should be protected as going to cart screen should go to login screen first and in login and register pass cartIds in body of login and register APIs

Screen shots are attached but not same design I want I need to follow design guidelines of our current web
see artifacts: @spec/cartandaddress folder

**No errors thourough testing is needed**

**Need all functionality to be working with APIs and fully tested.**
