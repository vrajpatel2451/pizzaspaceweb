# Fixes needed for showing combos as per testing

1. UI for combo is pathetic

   - UI should be same as how we select variant just the combination product is correct
   - +more item is missing just do it what we used to do for variant and combo
   - selected 1 chip is weird ui just do it what we do addon no need for that just highlight that row
   - 2/2 is weird do it what we do for addon group

2. on click on customise we do not show anything dialog is not opening. so not tested further flow please fix that.
