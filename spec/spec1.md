Need to build UI theme for food commerce like zomato with components
Context: Brand is Pizzaspace which opened newly in London, UK

1. Need proper color themes of orange + #0e182b(dark bluish) something I will give u screenshot. Dark and light mode will be good
2. Need to build pure components list is below mentioned
   Button, Input, TextArea, Select, SearchSelect, AsyncSearchSelect, CreatableSelect (try if we can create single component for one for all select), IconButton, Checkbox, RadioGroup, RadioButton, Switch, Accordion, Menu/Popover, Dialog(should contains titlesting, actions, divider hide/show and children take good examples from web), Bottomsheet, Toasts, Chip, Tabbar, ToggleButtonGroup, QuantityIncrementor, Shimmers/Loaders, Icons etc.
3. Need to build full Typography with montserrat fonts
4. Need one screen to represents full showcase of all components
5. Pricing wrapper, Date Wrapper, etc. should be in UK format
